export type User = {
  avatar_url: string;
  html_url: string;
  login: string;
  url: string;
  id: number;
};

export type UserDetails = {
  bio: string
  blog: string;
  company: string;
  email: string;
  login: string;
};
